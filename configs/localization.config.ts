import enGB from "../localizations/en-GB";
import plPL from "../localizations/pl-PL";

const localizationConfig: Record<string, any> = {
    'en-GB': enGB,
    'pl-PL': plPL,
}

export default localizationConfig;